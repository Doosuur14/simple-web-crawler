# simple-web-crawler
 This project implements a simple web crawler written in Python.  The crawler downloads at least 100 text-based web pages from a predefined list of URLs and saves them locally as HTML files while preserving their original markup. It also generates an index file that maps each downloaded page to its corresponding URL.
